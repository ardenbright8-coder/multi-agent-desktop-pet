# 多Agent桌面宠物 · 组合清单

> **现行版：v0.1.23**（2026-08-13）
> 版本号只写在这一份里，各块不单独编号（机制 26）。

## 书签台并入·第一步：新区块框架（2026-09-09，源码已提交·**未打包上机**）

用户拍板（2026-09-09）：随手记/书签不做独立常驻应用，**并入桌宠当完全隔离的大板块**——一个壳两个项目，省一份常驻内存，后期想拆整块搬走。总体架构（三家方案交叉验证后定）：电脑主体（本应用）+ 手机收发箱 APP（后续独立项目）+ 阿里云自建 ntfy 中继（下一步部署）+ 二阶段可选隧道（Cloudflare Tunnel 或 frp，用户有域名和 VPS）。

本步做了什么（新区块 `src\bookmark\`，挂自己的 AGENTS.md）：

1. **本地库 `store.ts`**：JSON 原子写（`<appDataRoot>\bookmark\bookmarks.json`，刻意独立于 data\）；坏档挪 `.corrupt-<时间戳>` 存证后空库重来（照事件日记先例）；`dedupeKey` 相同的重复投递只记一条（手机离线重发去重用）。
2. **面板 `panel-window.ts`**：独立 BrowserWindow，Win11 毛玻璃（`backgroundMaterial: "acrylic"`，构造 try/catch 回落无材质），置顶用 **floating**（不抢桌宠的 screen-saver 层），skipTaskbar，全局热键 **Alt+S** 呼出/收起（testMode 不注册，自测不抢真机热键）；面板里链接一律丢系统浏览器。
3. **托盘入口**：`PetTrayActions` 加可选 `openBookmark`，app.ts 注入 `toggleBookmarkPanel` —— **pet 块不 import bookmark**（反向依赖不存在，边界保持单向）。
4. **界面 `renderer\`**：输入框（Enter 记、Shift+Enter 换行、自动识别网址）+「交给谁」chips（Claude/Grok/Pi/Hermes + 自定义）+ 最新列表（删/改标记），基准字号 15px（v0.1.14 视力硬需求）；函数名一律 `bm` 前缀防跟 pet renderer 全局撞名。
5. **测试**：`testkit\tests\bookmark-store.test.ts` 六项（增删查/去重/落盘/坏档隔离/脏数据过滤）。

**隔离铁律**（写进 bookmark\AGENTS.md）：书签块只准依赖 shared；收信失败/崩溃不许连坐桌宠本体；数据绝不进桌宠事件索引。边界检查已把 `bookmark` 登记进 allowed（只认 shared）、`LogArea` 加了 `bookmark` 域（日志分域照旧）。

验收：`npm run build` ✅ / `npm test` 35+4 ✅（含书签六项）/ `node 检查边界.mjs` ✅ 52 文件 / `npm run controls` ✅（14/6/6/11，与 v0.1.23 基线一致）/ `npm run smoke` ✅。git 提交见本仓库。**未打包上机**——打包后人工验：Alt+S 呼出/收起、毛玻璃质感、记/删/改交给谁、托盘「书签台（随手记）」入口。

🚨 踩坑记录：编译后 `panel-window.js` 住在 `dist\bookmark\` 里，preload/renderer 的路径**相对 `__dirname` 拼**（第一版多套了一层 `bookmark\`，靠自查抓的）。

## v0.1.23：气泡条 → 座位环境 → 木框浮窗（2026-08-13 ~ 08-14，一整个大版本）

用户拍板方向：**窗口做小、环境定好、小人表情（4 状态+休息）用户自己提供素材**。这版把「气泡条」彻底改成「教室环境 + 工位小人」，又从横版大窗改成**物理 480×290 右上角小浮窗**，最后**移除桌面幼苗、功能集成到窗口**。

### 历程（按时间）

**第一版（网格座位）**：状态框下面 3×3 固定座位格（椅子/工位台/小人三层）。窗口 410→310。用户看完说「太简单，环境要一模一样」→ 推翻。

**第二版（教室背景 + 工位对齐）**：窗口背景 = 用户原型图（像素教室 1672×941），9 个座位按 vision 标定的工位中心百分比对齐（3 列 24/48.5/74，3 排 39/62.5/89，最前排略偏左）。桌椅/显示器都在背景图里，座位只渲染「有人的」（小人+状态灯）。窗口 760×360（图 640 + 右侧 120 幼苗区）。

**椅子贴片**：用户要「把椅子抠出来当素材」。试了 PIL flood fill / OpenCV GrabCut 自动透明抠图——像素画渐变棕，椅子跟地板/桌面颜色纠缠、描边黑和显示器黑同色，**自动透明抠图不可行**。改用**椅子贴片**：按坐标从背景原位裁出 9 把椅子（`assets/seats/chair-0~8.png`，190×140 图内像素），保留原背景色、贴回原位像素差=0 无缝，但椅子成为独立 DOM 元素（可换素材/动画）。

**完成小人表情**：用户发「完成」状态小人图（绿小人+黄星+橙放射线），代表 done 状态（替代绿点）。用 Python+PIL 抠图（v10：flood fill + 颜色键控，**踩坑：cv2 是 BGR，色值写反导致键控删错颜色**），`assets/avatars/done.png`（1040×602 透明 PNG）。素材回落链：`<agent>-<状态>.png` → `<状态>.png`（通用）→ `<agent>.png` → 隐藏（boot.js capture error 委托实现）。状态灯视觉去掉（用户：小点可以不要，功能保留）。

**第三版（物理 480×290 木框浮窗，用户 2026-08-14 定规格）**：
- 窗口物理尺寸固定 480×290，按 `display.scaleFactor` 换算 Electron 逻辑像素（这台 179% → 272×168 逻辑）——**screen 在 app ready 前访问会挂，尺寸在 createPetWindow 里算（ensurePetWindowSize）**
- 默认位置右上角，距工作区顶/右 16 物理像素（`PET_PHYSICAL_MARGIN`，`defaultWindowPosition` 加 margin 参数）
- 场景原图不压缩，`object-fit: contain` 完整显示不裁切
- 画框：胡桃木圆角（border 1px、圆角 14、内边距 8、阴影）→ 后改用**用户生成图 v2（自带圆角木框，四角白色 PIL 处理成透明）**
- 座位尺寸改百分比（跨缩放自适应），状态框缩成顶部小胶囊（92px），幼苗缩到 0.3

**桌面幼苗移除 + 功能集成窗口（2026-08-14 用户拍板）**：
- 幼苗**样式/动画全部移除**（`.pet { display: none !important }`，元素保留避免 pet.js/boot.js 报错）
- **拖窗口**：按住画框（`.scene-frame`）拖动移动整个窗口（pet.js `beginPetDrag(event, targetEl)` 通用化，boot.js 绑画框 pointerdown，排除座位/状态框/菜单）
- **右键菜单**：右键画框任意处弹功能菜单（`#pet-menu`：详情/设置/藏起来/测试通知/打开日志/退出），新增 ipc `window:show/open-logs/quit` + preload `showWindow/openLogs/quitApp`
- 托盘保留，功能跟右键菜单一致
- 设置面板的幼苗大小/活泼滑杆保留但无效果（幼苗已隐藏）
- 面板弹出时画框+幼苗一起隐藏（:has 规则）

**新背景 v2（2026-08-14）**：用户换图（工作间场景，1670×941，9 工位，右上门），用 exec-bad4dcc7 版（圆角木框），四角白色 flood fill 透明化存 `assets/classroom.png`。座位坐标沿用（内容一致）。

### 当前状态（2026-08-14 04:15 沉淀）
- ✅ 已上机 release-0.1.23，验收全绿：`node 检查边界.mjs` / `npm test` 29+4 / `npm run controls`（buttons=14 dynamic=6 inputs=6 window=11）/ `screenshot` / `lifecycle` / `verify-seats.mjs`（座位对齐新背景、完成小人回落、面板让位）
- ✅ 桌面幼苗已移除、功能集成窗口（拖/右键菜单/弹窗保留）
- ✅ 新背景（用户图 v2 圆角木框）已替换

### 🚨 待办（用户最后反馈，未处理）
1. **座位 hover「虚框」去掉**：用户说点椅子/小人能跳出来就行，hover 的边框+背景高亮多余。座位定位缩成**小圆点**（贴合椅子即可），不搞大透明区域。
2. **椅子大小/错位**：用户反馈背景里椅子大小不一致（透视）、场景跟桌面有错位感——确认是否跟透明区域/contain 缩放有关，需要时把窗口改成不透明或调整。
3. **休息（idle）状态表情**：表情映射缺 idle，用户会提供 `idle.png`，接进 `SEAT_EXPRESSION`。
4. **素材库接入**：用户正在让别的 agent 往素材库导素材（working/error/done/waiting/idle 图），导好后接上（命名 `<agent>-<状态>.png` 或 `<状态>.png` 放 `src/pet/renderer/assets/avatars/`）。

## v0.1.22：各 Agent 状态气泡条（2026-08-13）

用户：想要一排小气泡，当前有几个 Agent / CLI 终端在工作就亮几个，竖着往下延伸，最多 9 个；
每个气泡带灯色（绿=工作中 / 黄=休息中 / 红=卡顿·故障），点一下直接切进那个终端。

**做在 `pet\renderer\`（shell.js 渲染 + boot.js 点击 + index.html/styles.css）+ `pet\window.ts`（窗口长高）**：

1. **气泡 = 一个开着的会话**（不是按 Agent 合并）：开几个 Pi 窗口就亮几个 Pi，点哪个切哪个。
   灯色判定：working/thinking/waiting = 绿；idle/done = 黄；error = 红；
   **working/thinking/waiting 超过 5 分钟没收到任何事件 → 红（卡住该去看了）**（阈值 5 分钟，用户让我定）。
2. 竖排挂在**状态框下面**，跟状态框同一侧（换边跟着换），最多显示 9 个（会话按状态优先级排序，挤掉最不重要的）。
3. **点气泡 = 切回那个终端**：复用 `focusAgent`（先按事件带的进程号，再按标题打分），带 agent/project/originPid。
4. 灯色靠**时间重算**，不靠事件：界面 10 秒自己刷一遍，5 分钟到了即使没有新事件也会转红。
5. 面板（询问/权限/抽屉/设置/完成弹窗）弹出来时气泡条整条让位（`:has()` 规则），不跟面板打架。
6. 窗口高度 265 → **410**：状态框（~105）+ 9 个气泡（26×9 + 间隔）要装得下；
   controls 里「平时窗口高度 ≤340」的断言跟着放宽到 ≤440（这台机 778 高的工作区还有 350+ 能挪）。
7. 顺手修了 controls 两条**老 flake**（v0.1.21 就有，跟本功能无关）：
   ① 托盘「发一条测试通知」固定发 demo-session 的 permission.requested，会变成残留 pending，
     后面的面板测试一关当前窗 advanceToNextPending 就把它顶出来 → 当场清掉（跟完成弹窗段一个套路）；
   ② 滑杆 Home/End 断言从单次快照改成轮询（键盘事件送达有延迟）。

**验收**：`node 检查边界.mjs` ✅；`npm test` 29+4 ✅；`npm run controls` **连跑 3 遍全过**（修完老 flake 后）；
`screenshot` ✅ `lifecycle` ✅ `smoke` ✅ `single-instance` ✅ `integration:smoke` ✅。
**一次性实机验证**（`scripts\verify-bubbles.mjs`，npx electron 跑）：4 个不同状态会话 → 气泡 4 个、
灯色 stuck/working/stuck/resting、排在状态框下；点 Hermes 气泡 → focusAgent 收到 {agent:hermes}；
权限面板弹出 → 气泡条隐藏。截图在 `.runtime\verify-bubbles\bubbles.png`。

## v0.1.21：幼苗点了/写了 yes 就算数（2026-08-13）

用户：终端选了确认，幼苗没有自己填；点两次确定没反应。对照 Clawd `src/permission.js`（once/always/reject 回传）和 `src/bubble-renderer.js`（Other 文本框）。

**根因**：Grok 自己的英文三选项和幼苗是两套。终端点了幼苗不走；幼苗点了终端也不收。钩子原先只有「允许一次/拒绝」，自己填关着，写 yes 也当拒绝。

**修法**：钩子改成三选项 + 自己填；写 yes/允许/同意 放行，选「以后都允许」记住。Grok 改成自己不问，让钩子等幼苗。面板源码：文本框有字就算自己填。幼苗不用重启就能弹出新选项（选项来自钩子事件）。点完会再弹回执：写了啥、选了「以后都允许」下次自动放行也会再告诉你一声。Grok 批计划那扇窗幼苗批不了，只能提醒回终端按 a。Grok 实际跑的是 Claude 配置里那条钩子，必须 timeout 180，崩了要放行并写日志；终端英文窗若还在，幼苗点完会替你按键。

**返回终端切错窗**：标题里有 Grok 就切，结果切到续枝面板、后来又切到浏览器。Grok 只认 `grok.exe` 命令行，续枝和浏览器排除。

**重启后旧窗是僵尸**：进程刚起来时，事件日记会把旧权限窗重放出来，但钩子已经不在等。这种窗一点确认就卡「正在交回」没人接。`session-store.dropRestoredPending()` 启动时一律清掉（有专项测试盯着）。

**上机（2026-08-13 03:45）**：`npm test` 29+4 全过；`node 检查边界.mjs` ✅；`npm run pack` 产出 release-0.1.21 已启动，桌面/开机自启快捷方式都指向新版。git `1eb505e`。

**v0.1.21 补：返回终端切错/切不到（2026-08-13 04:0x 实机）**：
用户测「返回终端」时一直 NO_MATCH。查进程树：pi 跑在 `启动 Pi Agent.ps1`（双击 explorer 启动的 powershell → cmd → node）里，窗口是 **PseudoConsoleWindow（ConPTY 伪控制台）**，`MainWindowHandle=0` 且标题为空 —— 旧 focus-agent 的 Get-Process 主窗口判断和标题打分都找不到它。
修法（`src\pet\focus-agent.ts`）：改用 **EnumWindows 按 PID 枚举可见窗口**（含伪控制台）；找不到有标题窗口时，**进程命令行含 agent 特征**（如 `Pi Agent` / `pi.cmd`）也认；全局搜索同样枚举窗口 + 命令行兑底。已打包进 release-0.1.21 并实测（日志 `回终端已切到 powershell pid=44084`）。git `c73f130`。

**实测结论（2026-08-13 04:1x）**：真实可回传弹窗（pi 的 desktop_pet_question）选项能点、确认有反应、交回正常；「无回传通道」的模拟窗本来就没有确认按钮（fallback 提示回终端），不是坏按钮。

**v0.1.21 补二：弹窗排队 + 输入框不丢字（2026-08-13 04:3x 实机）**：
用户开五六个 Pi 窗口，多窗口同时要确认时只显示一个，其余“像没弹窗”。根因：`panel.js` 处理完当前 pending 后只等下一次快照（没新事件就不弹下一个）；且每次快照都重建整个面板 innerHTML，用户在「自己输入」里打的字被其他窗口的状态事件重渲染清掉（输 1234 过一会就没了）。
修法：新增 `advanceToNextPending()`——确认/关闭/交不回去后主动找下一个未处理 pending 弹出来（像批文件逐个过）；`renderInteraction` 同一 eventId 不重建选项/输入区（保留用户输入）。已打包上机并实测：3 个排队窗连续点确认，每次交回 0.04s、下一个自动弹出。git `待提交`。

## v0.1.20：返回终端按进程号切 + OpenCode 权限终于能弹（2026-08-13）

用户：分屏时点返回切到别的桌面页；OpenCode 终端已经在问 Allow，桌宠没弹。

**返回终端**：以前按窗口名字打分，分屏/多桌面会切错。现在事件带上发事件那个进程号，优先切那个进程的窗口（学 Clawd 的 pid 思路，不抄它那套缓存）。按钮改叫「返回终端」。

**OpenCode 权限没弹**：`opencode.json` 里 `plugin` 只写了 Clawd，显式名单会盖掉自动发现，桌宠插件根本没加载。已把桌宠插件加进名单，**OpenCode 要新开窗口才吃到**。

## v0.1.19：确认交不回去就切回原窗口 + 接上 Grok Build（2026-08-13）

用户：点确认有时冒一堆「返回 Agent 失败」看不懂；另外要把桌宠接到 Grok Build。

**确认失败**：OpenCode 插件死了/换实例后点确认会等 20 秒，再丢一句看不懂的红字。现在 8 秒交不回去就收起面板，直接把对应窗口拉到前台，人去原窗口再点一次就行。

**Grok**：`~\.grok\hooks\multi-agent-desktop-pet.json` 接 SessionStart／工具／完成／失败／权限提醒。Grok 没有回传通道，权限走「回原窗口」。关掉 Grok 顺手扫 Claude hook，免得一条事件报两次。

## v0.1.18：修完成弹窗乱弹 + 回原窗口真切回去（2026-08-13）⭐

用户：干完才该弹；点「回原窗口」有时没反应；完成通知会乱弹。

**乱弹两处根因**（对照 8-12 / 8-13 日志）：
1. OpenCode 插件「工具跑完 8 秒就报完成」——工具之间一停就当干完，还会跟真正的 `session.idle` 叠成双发；失败后 idle 又把状态改成完成。昨天 OpenCode 一家就报了 108 次完成。
2. 界面用 lead 身份变化弹窗——已经点掉的完成会话一回到队首，再弹一次。

**修法**：
- 插件：一轮只报一次完成；信 `session.idle`；失败后 idle 不许再改成完成；兜底从 8 秒改成 90 秒（只防 idle 彻底丢了）
- 界面：只在某个会话**新进入 done** 时弹；已经是 done 的再当 lead / 再报一次完成都不弹
- 「知道了，回原窗口」真去切对应 Agent 窗口（按标题/进程名打分），不再只关弹窗

**验收**：`npm test` 要过（含完成闸门 + 回原窗口打分）；`controls` 加了完成弹窗不重弹的回归。插件改完 **OpenCode 要新开会话才吃到**。

## v0.1.17 补二：修 OpenCode 插件完成判定 + 全链路模拟器验收（2026-08-12）⭐

**为什么用户一直收不到完成通知**（实机抓到）：插件只在 OpenCode 发 `session.idle` 时上报"完成"，但连续干活时 OpenCode 几乎不进 idle → 完成事件一条都发不出去 → 弹窗从未触发。

**修法**（`integrations\opencode\multi-agent-desktop-pet.mjs`，已同步 `~\.config\opencode\plugins\`）：
- 新增 `scheduleCompletion`/`cancelCompletion`：**工具跑完 8 秒没有新的工具/思考动作 → 自动发 task.completed**
- thinking 开始（session.status busy）、新工具开始（tool.execute.before）都会取消计时，不会误报
- session.idle / error / deleted 保留原有逻辑并先取消计时
- ⚠️ 插件启动时加载一次：**要重启 OpenCode（或新开会话）才生效**

**全链路模拟器验收**（CDP 真实鼠标事件 + LocalIpcClient 扮演 Agent，22 项全过，脚本在 `C:\Users\嘉楷\AppData\Local\Temp\opencode\verify-full-sim.js`）：
- A 询问链路 6 项：面板弹 → 问号 waiting 姿态 → 真实鼠标点选项被选中 → Agent claim 拿到答案 `optionIds:["a"]` → complete → permission 日志"答案已交回" → 面板收起
- B 权限链路 6 项：同上，徽标"权限"、选"允许一次"、交回后姿态回 working-typing
- C 完成弹窗 3 项：弹出 + 摘要 + 点"知道了，回原窗口"收起
- D 超时僵尸 5 项：提交后 20 秒没人接 → 面板自动收起 + pending 清掉 + 日志记录"没人接，退回原窗口"
- E 完成弹窗 2 项：新完成重新弹 + 点"关闭"收起
- 插件完成判定 4 项（`verify-plugin.js`）：工具后 9 秒发 completed / 连续干活不误发 / 停顿后发 / thinking 取消

**踩过的测试坑（记着别再来一遍）**：① 裸 `question.asked`（不带 interaction）会把桌宠锁 waiting 2 小时压住一切——测试必须发完整 interaction；② 插件 discovery 拼 `%APPDATA%\AgentPetHub\runtime`，隔离测试要用 junction 把目录名接上；③ claim 的 sourceInstance 必须与事件一致；④ `.interaction-option:first-child` 选不中（fieldset 第一个子元素是 legend）。

## v0.1.17 补：治欠账 —— 面板互斥收进调度口（2026-08-12）

8 处界面互调清零（拆分时就记着的欠账，见旧版 pet\AGENTS.md）：

- `shell.js` 新增三个调度口：**`openPanel(which)`**（开一个面板关其他全部：抽屉/设置/询问权限/完成弹窗）、**`restoreInteractionAfterPanelClose()`**（关面板后把待处理询问放回来）、**`refreshPetPose()`**（面板开关后的姿态刷新）
- `drawer.js` / `panel.js` / `pet.js` 全部改成只报「我要开了」，不再互相点名；`检查边界.mjs` 的 `debt` 清空
- 验收：边界 ✅ **0 欠账**；`npm test` ✅ 27/27；`controls` ✅；`screenshot` ✅；**CDP 实测 5 项全过**（完成弹窗弹出/开抽屉让位/开设置让位/收设置全关/询问让位）
- 打包覆盖 release-0.1.17 已上机，git `b1a255d`

## v0.1.17：完成弹窗 + 修僵尸权限面板（2026-08-12）⭐ 干完必须看得见

用户两条硬要求：① 干完活必须弹窗通知（跟权限/询问一个壳），不是小黄条；② 权限面板点「同意」没反应——查出来是僵尸面板。

**完成弹窗**（`pet\renderer\`）：
- 新面板 `#completion-panel`：复用 interaction-panel 壳（绿色完成徽标），底部两按钮——**「知道了，回原窗口」**（收起弹窗去 Agent 那边看结果）／**「关闭」**
- 触发：lead 会话变 `done`（task.completed）就弹；任务接任务时跳跃会被盖，弹窗不会
- 互斥：弹窗开着时来询问/权限 → 弹窗收起让位；开抽屉/设置 → 收起弹窗
- 同状态不重弹（changed 只在状态切换时触发），点掉后新完成再来会重新弹
- 用户没点就一直留着（这就是"通知"的意义）

**僵尸权限面板**（`events\` + `interaction\`）⭐ 实机抓到：
- 症状：用户点「同意」后等 20 秒没反应——permission 日志显示 `答案等了 20000ms 没人接，退回原窗口`
- 根因：**超时后 pendingInteraction 没清**。Agent 那头死了/断了（那次 OpenCode 实例已经不在了），broker 超时只把状态标 failed，pending 还挂在会话上（waiting 状态 2 小时 TTL），面板永远留着 → 用户再点旧面板 → 又等 20 秒 → 又超时 → "点了没反应"
- 修法：`session-store.expireInteraction()` 新增——broker 超时回调里把该 pending 清掉（会话回 idle），面板自动收起。hub 装配注入。配套单元测试：超时后 onExpired 必须被调
- 附带发现：`question.asked`/`permission.requested` 若不带完整 interaction 数据，兜底也会生成可点的假请求——以后测试别发裸的 question 事件（这次就是被它锁死 2 小时 waiting 压住一切）

**验收**：边界 ✅ 无新越界（欠账仍 8）；`npm test` ✅ 27/27；`npm run controls` ✅；`npm run screenshot` ✅；**CDP 实测完成弹窗 6 项全过**（初始隐藏/完成弹出/working 不盖/回原窗口收起/新完成重弹/询问互斥）。已 git 提交 `ddda31b`。

## v0.1.16：动效升级 —— 疑问问号、敲电脑、完成星光、呼吸（2026-08-12）

用户要求：参考 GooglePiggy（`Myf-ricey/GooglePiggy_DesktopPet`，Clawd 同款的 GIF 小猪桌宠）的动效，把状态动效做丰富。

**澄清**（2026-08-12 用户当场纠正）：问号不是常驻按钮，是**疑问状态**的信号——Agent 发来询问/权限请求（waiting）时出现，平时没有。

四处改动，全在 `pet\renderer\`：

1. **疑问问号强化**：waiting 时头顶问号从「白底小字摇摆」升级成**黄底大问号弹跳**（`question-bounce`，1.0s 弹→落→小弹循环），跟状态框的 waiting 黄点呼应。学小猪 question 动效的「弹跳不止」感。
2. **敲电脑姿态**：新增 `working-typing`——双臂 0.8s 快速上下敲（键盘），面前像素 0.5s 快闪（屏幕输出），眼睛眯起盯屏。**working 状态的主姿态从 running 改成它**（用户原话「干活的时候，比如他自己敲电脑」）；ambient 随机候选也加了它。
3. **完成星光**：jumping 时 6 颗彩色像素（黄/嫩芽绿/米白）从幼苗身上迸出上飘消失（`sparkle-burst`，跟跳跃同节奏播两轮，逐颗错开延迟）。学小猪 success 的 sparkle/firework 特效。
4. **呼吸升级**：idle 从「上下浮 3px」改成**squash 呼吸**——压扁 0.97 → 回正 → 拉伸 1.02（正弦感），底部锚定，像真的在喘气。学小猪的程序化呼吸动画思路。

**验收**：`node 检查边界.mjs` ✅ 无新越界（欠账仍 8 处）；`npm test` ✅ 26/26；`npm run controls` ✅ `CONTROLS_OK buttons=14 dynamic=6 inputs=6 window=11`；`npm run screenshot` ✅（姿态列表补了 working-typing）。已 git 提交 `ec6130e`。

## v0.1.15：确认按钮「点了没反应」（2026-08-12）⭐ 测试为什么会漏

用户：「这个框我疯狂点它点不动，确认点了很多遍都没反应。」

**真相：按钮没坏，是选项被挤出视野了。** 上一版字号放大 ＋ 面板让出幼苗顶带，
两下叠一起把正文区压得太扁，两个选项滚到视野外。用户点确认 → 代码其实弹了
「请先为每个问题选择一项」→ **但那句提示也在同一个滚动区里，同样看不见** → 手感就是按钮是死的。

🚨 **这条最值钱的教训：自动测试为什么一直是绿的。**
`controls` 点选项前会先 `scrollIntoView()` 自动滚过去，所以测试永远点得着——
**测试把用户不会做的事替它做了**。这类「能点到 ≠ 看得到」的问题，断言必须写成
「面板一打开，第一个选项就在视野里」，而不是「点得动」。

**四处一起改**：
1. 错误提示**移出滚动区**，钉在按钮上方的固定区 —— 报错必须永远看得见。
2. 点确认没选时，**自动把没选的那题滚到眼前**。
3. 幼苗顶带 `--pet-band` 改成**按幼苗当前大小实时算**（`8 + 176×缩放 + 12`），
   不再死占最大档的 262px —— 用户用 100% 时面板能多出 78px 正文。
4. 面板窗口 680 → **740** 高（工作区 778，留 38 余量）。

**新增两条回归**：面板一打开第一个选项必须在视野内；错误提示必须在滚动区外面。

## v0.1.14：字号放大、面板不遮宠物、关掉误缩放、右键进设置（2026-08-12）

用户五条：

1. **字看不清** —— 面板正文 12px → **15px**，选项标题 11px → 15px，选项说明 10px → 13px，
   标题 21 → 23px，输入框 11 → 14px，状态框正文 13 → 15px。用户视力不好，这是硬需求。
2. **不管哪个框弹出来都不许遮住宠物** —— 新增 CSS 变量 `--pet-band: 262px`，
   询问/权限面板、事件抽屉、设置面板**一律从这条线以下开始**。
   配套：面板展开改成**往下长、窗口左上角不动**（幼苗贴窗口顶，所以纹丝不动）。
   `controls` 加断言：面板顶必须 ≥ 幼苗底。
3. **宠物跟状态框别分那么开** —— 间距收紧（幼苗 left/right 14 → 4，框 8 → 6）。
4. **关掉 Ctrl +/-** —— 误按会把整个桌宠缩得莫名其妙。
   `setVisualZoomLevelLimits(1,1)` ＋ `zoom-changed` 复位 ＋ 拦 Ctrl +/-/0 ＋ 拦 Ctrl+滚轮。
   幼苗大小走设置里的滑杆，不要第二套缩放。
5. **右键幼苗直接进设置** —— 幼苗脚下那排按钮撤走后，得留个快捷入口。

## v0.1.13：幼苗能顶到屏幕上沿，状态框改到侧边可换边（2026-08-12）

用户四条要求，一起做了：

1. **幼苗要能顶到上面** —— 状态框原来焊在幼苗**正上方**，把幼苗顶得离屏幕顶老远。
   现在框改到**侧边**，幼苗还从「底部对齐」改成「**顶部对齐**」，100% 时离窗口顶只有 8px。
2. **框跟着幼苗的位置换边** —— 幼苗在屏幕左半边 → 框放右边；在右半边 → 框放左边。
   框永远朝屏幕中间展开，不会顶出屏幕，也不压着幼苗。主进程 `syncNoteSide()` 每次挪窗口算一次。
3. **能手动换边** —— 托盘「聊天框换个边」。**手动换过就钉住，不再自动翻**（`noteSidePinned`）。
4. **撤掉幼苗脚下那排「详情／设置／隐藏」** —— 常驻占地方还挡视线，用得又不频繁。
   全挪进托盘菜单：详情（事件与搜索）、设置（大小与活泼程度）、藏起来。

**另外加的**：🆕 **永远压在最前面** —— 置顶层级从 `floating` 提到 **`screen-saver`**（Electron 最高档），
再加一道每 3 秒的重申和失焦时重申：有些全屏程序会把置顶窗口挤下去，光设一次守不住。
`controls` 加断言盯着 `isAlwaysOnTop()`。

### 这一版踩的两个坑（都是测试当场抓的）
- 🚩 **幼苗区一度占满整个窗口**（`inset: 0`），而它带着 `data-interactive`，
  于是**整块 435×265 都在吃鼠标**。改成把「可点」收窄到 `#pet` 和 `#status-note` 本身。
- 🚩 **幼苗放到 140% 会长出窗口**：窗口高度按 100% 算的，放大后溢出到窗口顶部，
  把本该穿透的角落也占了。窗口尺寸改成按**最大缩放**算：`8 + 176×1.4 + 8 ≈ 265`。

## v0.1.11 ~ v0.1.12 ⭐⭐ 窗口自己越长越大（靠日志抓到的）

用户：「左右滑着滑着就跑到最下边，停在下边很难再往上。」

**日志一贴就水落石出**（这条 bug 不看日志根本猜不到）：
```
窗口=92,0  290x363     ← 设计尺寸是 220x304
窗口=102,0 412x474
窗口=51,0  614x636
落点=0,-190 尺寸=940x961
窗口越界被拉回 0,-190 → -191 → -193 → -195
```

**根因**：这台机缩放 **179%（非整数）**，Electron 每次 `setPosition` 都会把窗口尺寸四舍五入涨一点点。
v0.1.6 加的 `safeWindowSize()` 拿 `getBounds()` 的**实测尺寸**当边界基准 —— 涨了之后又被当成新基准，
**自己喂自己滚雪球**，220×304 一路长到 940×961。

窗口一旦长得比工作区（778）还高，clamp 算出的上界就比下界还小，窗口被一路推出屏幕顶部
（末尾那几行 `-190 → -195` 就是它在往上爬）。用户手感就是「只能往下拖，永远上不去」。

**三处一起修死**：
1. `safeWindowSize()` **只认设计尺寸**，绝不再拿 `getBounds()` 当基准。
2. 新增 `moveTo()`：所有挪窗口的地方一律走它，用 `setBounds` **把尺寸一起写死**，不给膨胀机会。
   （`setPosition` 在本文件已全部退役。）
3. `clampWindowPosition` 加防御：窗口比屏幕还大时（上界 < 下界）贴左上，别把窗口推出屏幕。

**回归保护**：`controls` 里连拖 60 次，窗口宽高**一个像素都不许变**。

**顺带**：默认落脚点从「贴着底边」改成「屏幕高度约 2/3 处」（用户指定），矮屏幕上会被底边规则接住。

## v0.1.8 ~ v0.1.10：加了运行日志（2026-08-12）

以前只有崩了才往 `agent-pet-error.log` 写一行，平时什么都不记，排障全靠猜。现在有正经日志了。

### 日志在哪
```
%LOCALAPPDATA%\我的App日志\多Agent桌面宠物    app-yyyy-MM-dd.log          启动退出、本机通道、装接入、未捕获异常
    window-yyyy-MM-dd.log       建窗口、拖动起落、面板长大缩回、窗口越界被拉回、界面报错
    session-yyyy-MM-dd.log      收到的每条事件（agent/kind/会话/待不待处理）
    permission-yyyy-MM-dd.log   答案交回链路：交了、成了、超时退回
```
**托盘右键 →「打开日志文件夹」** 一键到现场。

### 这套是怎么定的
- **目录、级别、保留期** 照资源库 `60_公用资源_脚本管理\App日志（各App共用·出事能查到）` 那套规范：
  三级（调试/记录/出事）、轻档门槛=记录、2MB 换新、留 7 天/10 个、密钥自动遮、同消息连发只记前 3 条。
  判轻档的理由：出问题用户能直接摸到这台机（规范「规则层」第一问）。
  ⚠️ 那套规范的执行层是 PowerShell 的 `日志.ps1`，Electron 跑不了，所以 `src\shared\log.ts`
  是**同规范的 TS 实现**。改行格式要两边一起改。
- **按功能域分四本** 是照 **clawd-on-desk** 学的（它把 permission / session / focus / update 各写各的）。
  🚨 **它没有日志级别**，轮换也只是「超 1MB 砍掉前一半」——这两点没学，砍掉的日志找不回来。
- 想看调试级：环境变量 `AGENT_PET_LOG_LEVEL=调试`。
- 自测跑起来时日志进隔离目录（`AGENT_PET_HUB_HOME\logs`），不污染用户那本。

### 日志刚上线就抓到的 bug（v0.1.10）
`keepWindowOnVisibleDisplay` 在**面板开着时也保存窗口位置**，存进去的是「长大后」的坐标；
下次启动拿它当小窗口位置用，幼苗开机就跑偏到屏幕左上。
靠 `建窗口 位置=422,0` 这一行日志当场看出来的——**这就是加日志的价值**。
改法：`persistWindowPosition()` 在 `panelOpen` 时直接 return。

## v0.1.7 修了什么（2026-08-12，第四轮实测）⭐ 窗口尺寸这一版

用户反馈「只能左右平移，上下动不了，应该全屏都能滑吧」。**这不是拖动坏了，是窗口太高**：

这台机 3440×1440 物理、缩放 179%，**Electron 眼里的工作区只有 1922×778**。
窗口按面板尺寸做成 440×680（算上系统边框实际 456×710），
上下能挪的空间只剩 `778 - 718 = 60` 像素，左右却有 1458 —— 拖起来当然是「只能左右」。

**改法（参考 clawd-on-desk 的思路：幼苗窗口本身要小）**：
- 平时窗口只有 **220×304**，就包住幼苗那一块 → 上下能挪 470+，整屏随便跑
- 面板要弹时窗口才临时长到 **440×680**，往左上让出面板那部分，**幼苗在屏幕上原地不动**
- 面板关掉缩回小窗口，同样以幼苗为锚

📌 **界面 CSS 一行没改**：幼苗区是 `right/bottom` 定位的，窗口一缩它自然贴在小窗口右下角。

**连带修的两处**：
- `controls` 加断言：平时窗口高度必须 ≤340 且工作区还剩 ≥200 的活动空间。
- `screenshot` 的姿态裁剪框原来写死 `{x:236,y:448,...}`（按 440×680 算的），窗口一小就截到窗口外，
  `capturePage` 直接抛 `UnknownVizError`。改成按 `#pet` 的实际位置算。

## v0.1.6 修了什么（2026-08-12，第三轮实测）⭐ 关键的一版

用户反馈「拖着拖着自动往下漂，一跑就到屏幕下面不见了」「框能不能别超出屏幕」。挖出三个叠在一起的坑：

### 坑一 · DPI：界面给的坐标是物理像素，setPosition 要的是 DIP ⭐⭐
旧代码用 `event.screenX/screenY` 算窗口位置。**那是物理像素**（0–3440 / 0–1440），
而 `win.setPosition()` 要 **DIP**（这台机 0–1922 / 0–778）。缩放 179%，两者差 1.79 倍——
鼠标越往下拖，窗口被送去的 DIP 坐标越离谱，一拖就漂出屏幕底部再也找不着。
**改法**：拖动坐标一律由主进程 `screen.getCursorScreenPoint()` 自己读（天生是 DIP），
界面只报「按下了／松开了」和按下时鼠标在窗口里的偏移。

### 坑二 · 窗口真实尺寸不是 440×680，而且还会飘
Windows 给无边框窗口留了一圈不可见边框：建的时候写 440×680，`getBounds()` 报 **456×710**；
挪一下位置又可能报 **461×715**（高 DPI 取整）。拿常量去算边界，右下角永远露十几像素在屏幕外——
面板右边被切、按钮够不着有它一份。
**改法**：`safeWindowSize()` 取「设计尺寸和实测尺寸的大者 + 8px 余量」，所有边界计算都走它。

### 坑三 · 窗口本来就允许超出屏幕
旧设计只保证「幼苗那一小块可见」，窗口大半在屏幕外是正常的。面板一弹出来就被切。
**改法**：整个窗口一律锁在工作区内（用户明确要求「框不要超出屏幕」）。拖也拖不出去。

**回归保护**：`controls` 里往屏幕外死拖两次（右下、左上），断言窗口四条边一条都不许越。

## v0.1.5 修了什么（2026-08-12，第二轮实测）

**面板右半边和确认按钮跑到屏幕外，点不着**。根因是个状态泄漏：
`setPanelVisibility` 拿 `positionBeforePanel` 是不是 null 当「面板开着没」的标志，
**这个值只要漏清一次**（渲染层重载、面板走别的路径关掉），后面就：
① 面板再也不会被拉回屏幕内 —— 你把幼苗拖出屏幕边缘之后弹面板，右半边就在屏幕外；
② `applyMouseMode` 以为面板一直开着，窗口**永久停在不穿透**，又开始挡鼠标。

改法三条：
1. 面板状态改用独立的 `panelOpen` 布尔值，不再靠 `positionBeforePanel` 猜。
2. 新增 `ensureFullyVisible()`：面板要显示时**每次都**把整窗拉回屏幕（幂等，重复调无害，漏调才要命）。
3. **主进程兜底**：`hub` 快照里只要有 `pendingInteraction`，就自己 `ensureFullyVisible()` 一次，
   不依赖界面那边的通知有没有送到。

顺手加了个 **托盘「把幼苗叫回来（回右下角）」** ——拖丢了一键回家，`controls` 里有断言盯着。

📌 **这台机的实测数据**（排查时量的，别再重量）：屏幕 3440×1440 物理、**缩放 179%**，
Electron 看到的工作区是 **1922×778 DIP**，窗口 440×680 装得下。
⚠️ 用 PowerShell 的 `GetWindowRect` 量窗口会被 DPI 虚拟化，数字对不上，**别拿它当证据**。

## v0.1.4 修了什么（2026-08-12，上机实测后）

v0.1.3 装上去用户一试，三条还不对：

1. **按得下去、拖不动**。根因：`beginPetDrag` 里调了 `event.preventDefault()`——在 **pointerdown** 上
   preventDefault 会把浏览器后续合成的 `mousemove`／`mouseup` 一起掐掉，拖动逻辑就再也收不到移动了。
   改法：去掉它（挡拖影改到 `dragstart` 上），并且 `pointermove` 和 `mousemove` 两个都监听。
   ⚠️ **为什么自测没发现**：`controls` 用 `sendInputEvent` 发的是原生 mouse 输入，不走"由 pointerdown 派生
   的兼容事件"这条路，所以照样能拖、测试照样绿。真机上才会踩到。
2. **气泡上写着"再点一下放下"**。`styles.css` 里 `.pet::after` 的 `content` 写死了旧交互的文案，
   v0.1.3 只改了 `aria-label` 和 JS，漏了 CSS。改成"松手放下"。
3. **「回原窗口处理」是个死按钮**。不支持回传的 Agent（Claude Code、Hermes）弹出的面板里，
   那个按钮写着字却是 `disabled`，点了没反应——用户当然"回不到客户端"。这同时违反了自己的 D-06
   「不放假按钮」。改法：按钮改成可点的「知道了，回原窗口处理」，点了收起面板。
   `controls` 加了断言盯着：这个按钮必须点得动、点了必须能关。

## v0.1.3 修了什么（2026-08-12）

用户报的两条，都在 `pet\` 这一块：

1. **桌宠挡住鼠标，底下的程序点不着**。根因：靠 `setShape()` 挡点击，但 Electron 官方文档明说透明区域点不过去，`setShape` 在 Windows 上对透明窗口不可靠，那 440×680 的透明矩形一直在吃鼠标。
   改法：默认 `setIgnoreMouseEvents(true, {forward:true})` 整窗让开，鼠标压到可点的东西上才临时收回。
2. **拖不动、拖一半找不着**。根因：旧交互是"点一下拿起、再点一下放下"，拖到别的屏幕后幼苗可能不在鼠标底下，就再也点不着、永远放不下，窗口卡在拿起态一直挡鼠标。
   改法：改成按住拖、松手放，配 pointer capture 防止拖快了丢事件，`window.blur` 兜底放手。

**加了回归保护**：`npm run controls` 现在会验"鼠标离开幼苗时窗口必须让开鼠标"，下次再改坏当场报红。
⚠️ **但穿透这件事的最终验证只能靠人**：自动测试发的是合成事件，绕过 Windows 真正的命中测试，测不出"底下的视频点不点得着"。

## v0.1.2 是什么

**功能跟 v0.1.1 一模一样，一个都没加没减。** 改的只有一件事：把按技术层分的目录（`main` / `renderer` / `shared`）改成**按功能分块**，顺手把两个胖文件拆开。

为什么切：改一个小地方，AI 得把 572 行的 `main.ts` 或 543 行的 `renderer.js` 整个读进来，慢且费 token。切完之后改哪块只开哪块。

## 结构长什么样（一眼看懂版）

```
src\app.ts              174 行   主入口：只装配，零业务逻辑
│
├── shared\             底座，谁都不许它反过来依赖谁
│     protocol.ts       382 行   事件协议、规范化、脱敏
│     paths.ts           28 行   所有路径常量
│     atomic-file.ts     14 行   原子写文件
│
├── events\             事件中枢与会话状态
│     hub.ts            221 行   收事件 → 存 → 索引 → 广播
│     session-store.ts  237 行   会话状态机和优先级
│     event-index.ts    152 行   中文实时索引
│     event-journal.ts   92 行   落盘与回放
│     simulation.ts      86 行   模拟事件（托盘那个按钮也走它）
│     renderer\drawer.js 99 行   事件抽屉界面
│
├── interaction\        询问与权限面板
│     presenter.ts       79 行   事件 → 面板内容
│     permission-explainer.ts 90 行   风险判断与大白话解释
│     broker.ts          90 行   等回答、超时、答案领取
│     renderer\panel.js 118 行   面板界面
│
├── channel\            本机通道
│     ipc-server.ts     181 行   命名管道服务端 + discovery
│     ipc-client.ts      77 行   客户端
│     ipc-handlers.ts    39 行   界面的 hub:* 请求
│     preload.ts         20 行   渲染层能看见的 API
│     cli-emit.ts        48 行   命令行发事件
│
├── pet\                幼苗和它那个窗口
│     window.ts         173 行   建窗口、位置、形状
│     window-ipc.ts      25 行   界面的 window:* 请求
│     tray.ts            43 行   托盘和菜单
│     window-position.ts 48 行   位置计算
│     focus-agent.ts            回原窗口切到对应 Agent
│     renderer\shell.js 142 行   界面底座＋总调度（必须第一个加载）
│     renderer\pet.js   132 行   姿态、拿起、外观设置
│     renderer\boot.js   68 行   绑事件、开定时器（必须最后加载）
│     renderer\index.html 148 行  styles.css 549 行
│
├── integrations\       四家接入
│     manager.ts         45 行   装插件和 Hook（插件源码在项目根 integrations\）
│
└── testkit\            自测台
      controls-test.ts  323 行   17 个按钮真实点击
      screenshot.ts      61 行   四面板 + 10 姿态
      lifecycle.ts       54 行   显示／收托盘／恢复
      single-instance.ts 33 行   第二次启动
      smoke.ts           23 行   管道与索引
      util.ts / host.ts / modes.ts  52 行
      tests\            9 份     25 项单元测试
```

**守门的三样**：七个夹口各一份 `AGENTS.md` 门牌；`检查边界.mjs` 用机器卡住谁不许调谁；本文件记版本和改动。

## 下次要改东西，怎么走

1. 照上面的表定位到**一个**夹
2. 进那个夹先读 `AGENTS.md`（这块管什么、露出什么、有什么坑）
3. **只开那一个文件**改
4. 跑 `node 检查边界.mjs`，报红说明伸手摸了别人家的
5. 跑对应的验收命令（见各夹门牌）
6. 回本文件更新版本号和「改了什么」

## 分块到底省了多少（2026-08-12 实测）

| 干什么 | 老结构要读 | 现在要读 |
|---|---|---|
| 改幼苗姿态／随机动作 | `renderer.js` 543 行 | `pet\renderer\pet.js` 132 行 |
| 改询问面板的收答案逻辑 | 同上 543 行 | `interaction\renderer\panel.js` 118 行 |
| 改窗口位置／形状 | `main.ts` 572 行 | `pet\window.ts` 173 行 |
| 改托盘菜单 | 同上 572 行 | `pet\tray.ts` 43 行 |
| 加一套新自测 | 往 572 行里塞 | **新建 1 个文件，别人一行不动** |

算上还得读一份门牌（约 700 token），**一次改动省约 70~80%**。

⚠️ **代价说清楚**：源码总行数从 3737 涨到 4167（+11.5%），多出来的全是各文件的头注释和门牌指引。拿磁盘换 token，值。

🚩 **最要紧的一条认知**：拆完之后改一个功能，**改的行数跟拆之前一模一样**。
**分块省的不是「要改多少行」，是「要读多少才敢下手」** —— 那才是 token 大头。

## 每块现在什么状态

| 块 | 管什么 | 上次动是什么时候 |
|---|---|---|
| `shared\` | 协议、路径、原子写 | 2026-08-12 拆分搬过来，逻辑没动 |
| `events\` | 事件中枢、会话状态、索引 | 同上（`simulation.ts` 是从 main.ts 抽出来的） |
| `interaction\` | 询问与权限面板 | 同上 |
| `channel\` | 命名管道、界面 IPC | 同上（`ipc-handlers.ts` 从 main.ts 抽出来，controls 开关改成注入） |
| `pet\` | 窗口、托盘、幼苗界面 | **2026-08-12 v0.1.3 修点击穿透和拖动**（分块时是从 main.ts 抽的） |
| `integrations\` | 四家接入 | 同上 |
| `bookmark\` | **书签台（随手记板块·完全隔离）** | **2026-09-09 第一步框架（新区块）** |
| `testkit\` | 全部自测 | 同上（四套模式从 main.ts 抽成四个文件） |

## 这轮的验收（全绿才算切完）

| 命令 | 结果 |
|---|---|
| `npm run build` | ✅ 过 |
| `npm test` | ✅ 25/25 |
| `npm run smoke` | ✅ `SMOKE_OK events=2 hits=1` |
| `npm run lifecycle` | ✅ `LIFECYCLE_OK` |
| `npm run single-instance` | ✅ `SINGLE_INSTANCE_OK signals=1` |
| `npm run integration:smoke` | ✅ 四家全过 |
| `npm run screenshot` | ✅ `SCREENSHOT_OK` |
| `npm run controls` | ✅ `CONTROLS_OK buttons=17 dynamic=4 inputs=6 window=6` |
| `node 检查边界.mjs` | ✅ 新越界 0，欠账 8（都在清单里） |

**v0.1.3 之后八项重跑一遍，全绿**（`controls` 的 window 项从 6 条涨到 7 条，多的是点击穿透）。

## 打包与上机（2026-08-12 已完成）

- 现行运行正本：`release-0.1.15\win-unpacked\多Agent桌面宠物.exe`
- 开机自启快捷方式已改指它（带 `--skip-integration-install`）
- `npm run verify:package` ✅ —— 断言也跟着分块改了：lifecycle 去 `dist	estkit\lifecycle.js` 验、
  新增两条守着点击穿透（`pet\window.js` 必须有 `setIgnoreMouseEvents`、不许再出现 `.setShape(`）
- 🚨 **上机才发现的**：用户桌面上一直跑的其实是 `%LOCALAPPDATA%\Programs\multi-agent-desktop-pet\` 里的
  **v0.1.0 旧安装版**，不是项目正本。旧入口还在，误点就会跑到老版本上去。
